import * as React from 'react';

import GenerateTable from './GenerateTable';

const All: React.FunctionComponent = () => {
  return (
    <div>
      <GenerateTable course={null}></GenerateTable>
    </div>
  );
};

export default All;

